package me.andante.invulnerableload.api;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public interface InvulnerableLoad {
    String MOD_ID   = "invulnerableload";
    String MOD_NAME = "Invulnerable Load";
    Logger LOGGER   = LoggerFactory.getLogger(MOD_ID);
}
